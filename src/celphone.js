class Cellphone {
  constructor(serial, imei, responsable, assignmentDate) {
    this.serial = serial;
    this.imei = imei;
    this.responsable = responsable;
    this.assignmentDate = assignmentDate;
  }

  save(serial, imei, assignmentDate) {
    return idCodigo;
  }

  update(responsable, assignmentDate) {
    return 1;
  }

  findByCodigo(imei) {
    return imei;
  }

  findByDescription(description) {
    return Codigo;
  }
}
